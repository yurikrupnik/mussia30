import { nodeFirebaseAdmin } from './node-firebase-admin';

describe('nodeFirebaseAdmin', () => {
  it('should work', () => {
    expect(nodeFirebaseAdmin()).toEqual('node-firebase-admin');
  });
});
