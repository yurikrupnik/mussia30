// import { nodeStateMachines } from './node-state-machines';

describe('nodeStateMachines', () => {
  it('should work', () => {
    // expect(nodeStateMachines()).toEqual('node-state-machines');
    expect(1).toEqual(1);
  });
});
