// import { nodeXstateMachines } from './node-xstate-machines';

describe('nodeXstateMachines', () => {
  it('should work', () => {
    // expect(nodeXstateMachines()).toEqual('node-xstate-machines');
    expect(1).toEqual(1);
  });
});
