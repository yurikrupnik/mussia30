apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-cluster.fullname" . }}
  labels:
    {{- include "local-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "local-cluster.selectorLabels" . | nindent 4 }}
